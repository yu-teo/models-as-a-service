apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Unconfigured model: LLMIS + MaaSModelRef only (no MaaSAuthPolicy, no MaaSSubscription).
# Used by e2e tests to validate gateway default auth denies access (403).
resources:
  - llm
  - maas
